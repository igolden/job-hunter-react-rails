import React from 'react'
import classes from './Jobs.scss'

export const Jobs = (props) => (
  <div>
    <h2>
      Jobs:
      {' '}
    </h2>
  </div>
)

Jobs.propTypes = {
  //here we would place the props necessary for our application to work.
}

export default Jobs
